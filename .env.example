# ezyVet Base URL
BASE_URL=https://master.usw2.trial.ezyvet.com

# Test environment
TEST_ENV=trial

# ezyVet Test Department
TEST_DEPARTMENT=Master branch

# Browser settings
HEADLESS=false
SLOW_MO=0

# Test user credentials
TEST_USER_EMAIL=botai@test.com
TEST_USER_PASSWORD=Sunshine1

# Test admin credentials (same for now)
TEST_ADMIN_EMAIL=botai@test.com
TEST_ADMIN_PASSWORD=Sunshine1

# API endpoints (if needed for setup/teardown)
API_BASE_URL=https://master.usw2.trial.ezyvet.com/api

# CI/CD settings
CI=false
WORKERS=1

# Timeouts (in milliseconds)
DEFAULT_TIMEOUT=30000
NAVIGATION_TIMEOUT=60000
ACTION_TIMEOUT=30000

# Retry settings
RETRIES=0

# Video and screenshot settings
VIDEO=retain-on-failure
SCREENSHOT=only-on-failure
TRACE=on-first-retry
